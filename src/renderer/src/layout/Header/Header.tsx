import { ThemeSelector, NavigationMenu } from './components'

export const Header = () => {
    return (
        <header>
            <NavigationMenu />
            <ThemeSelector />
        </header>
    )
}
